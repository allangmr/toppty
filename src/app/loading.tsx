export default function Loading() {
  return (
    <main
      id="contenido"
      className="mx-auto flex min-h-dvh max-w-4xl flex-col justify-center px-4"
    >
      <p className="animate-soft-pulse text-3xl font-bold tracking-[-0.03em] text-muted-foreground">
        Cargando la tabla…
      </p>
    </main>
  );
}
