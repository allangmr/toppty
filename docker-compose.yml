services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: toppty
    ports:
      - "5432:5432"
    volumes:
      - toppty_pg:/var/lib/postgresql/data

volumes:
  toppty_pg:
